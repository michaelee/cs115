import org.junit.*;
import static org.junit.Assert.*;

public class TestLab2 {  
  @Before
  public void setUp() {
  }

  @After
  public void tearDown() {
  }
  
  @Test
  public void t() {
    assertTrue(true);
  }

  // @Test
  // public void simpleTest() {
  //   Square sq = new Square();
  //   int length = sq.getLength();
  //   assertTrue("Length not greater than 0", length > 0);
  //   assertEquals("Perimeter incorrect", sq.getPerimeter(), 4*length);
  //   assertEquals("Area incorrect", sq.getArea(), length*length);
  // }
}
