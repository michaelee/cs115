import static java.lang.Math.*;

public class SimpleMath {
  public int sumOfSquares (int x, int y) {
    int result = 0;
    
    // compute the sum of the squares of x and y
    
    return result; // don't touch this line!
  }
  
  public double reciprocal (int val) {
    double rec = 0.0;
    
    // compute the reciprocal of val
    
    return rec; // don't touch this line!
  }
  
  public double celsiusToFahr (double c) {
    double f = 0.0;
    
    // compute the fahrenheit equivalent of celsius temperature c
    
    return f; // don't touch this line!
  }
  
  public double fahrToCelsius (double f) {
    double c = 0.0;
    
    // compute the celsius equivalent of fahrenheight temperature f
    
    return c; // don't touch this line!
  }
  
  public double average (double a, double b, double c, double d, double e) {
    double avg = 0.0;
    
    // compute the average of a, b, c, d, e
    
    return avg; // don't touch this line!
  }
  
  public double variance (double a, double b, double c, double d, double e) {
    double variance = 0.0;
    
    // compute the statistical variance of a, b, c, d, e
    
    return variance; // don't touch this line!
  }
  
  public boolean divisibleByNine (int n) {
    boolean result = false;
    
    // decide is n is evenly divisible by 9
    
    return result; // don't touch this line!
  }

  public double distance (double x1, double y1, double x2, double y2) {
    double distance = 0.0;
    
    // compute the distance between the points (x1, y1) and (x2, y2)
    
    return distance; // don't touch this line!
  }
  
  public long yearsInSeconds (int years) {
    long seconds = 0;
    
    // compute the number of seconds in years -- ignore leap years
    
    return seconds; // don't touch this line!
  }
  
  public int age (int month, int day, int year) {
    int age = 0;
    
    // compute the age of a person born on month, day, year
    // Assume today's date is 2/13/2009
    
    return age; // don't touch this line!
  }
}